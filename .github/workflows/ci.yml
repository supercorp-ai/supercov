name: CI

on:
  workflow_dispatch:
  # The gate every change is expected to pass, on the change itself. It ran
  # only when someone remembered to ask for it, which meant a pull request's
  # green tick said the React and assertion-map jobs had passed and nothing
  # about `cargo fmt`, clippy, the workspace tests or the packaged assets.
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        node: [22, 24]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - uses: dtolnay/rust-toolchain@1.95.0
        with:
          components: clippy, rustfmt
      - run: npm ci
      - run: npm run check
