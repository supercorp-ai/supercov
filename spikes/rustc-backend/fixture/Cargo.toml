[workspace]
members = ["probe-macros"]
exclude = ["external-rules"]
resolver = "3"

[package]
name = "supercov-rustc-spike-fixture"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
external-rules = { path = "external-rules" }
probe-macros = { path = "probe-macros" }
